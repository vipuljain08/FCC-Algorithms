// capitalize single word
const capitalize = str => str[0].toUpperCase() + str.substring(1).toLowerCase()

capitalize("hello")   // Hello
capitalize("aWESOME")   // Awesome
capitalize("fREEcoDEcaMP")  // Freecodecamp
